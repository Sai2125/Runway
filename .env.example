# Copy to .env and fill in. Never commit .env.
TELEGRAM_BOT_TOKEN=
# Optional — real Claude extraction + friend voice (falls back to heuristics if empty)
ANTHROPIC_API_KEY=
# Optional — voice notes (falls back to "type it for me?" if empty)
GROQ_API_KEY=
